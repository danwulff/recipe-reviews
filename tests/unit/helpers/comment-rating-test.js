import { commentRating } from 'recipe-reviews/helpers/comment-rating';
import { module, test } from 'qunit';

module('Unit | Helper | comment rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = commentRating([42]);
  assert.ok(result);
});
