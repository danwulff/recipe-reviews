import { commentRatingStars } from 'recipe-reviews/helpers/comment-rating-stars';
import { module, test } from 'qunit';

module('Unit | Helper | comment rating stars');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = commentRatingStars([42]);
  assert.ok(result);
});
